# Do not forget
1. Run npm install
2. npm install -d sass
3. npm install react-router-dom

## How to start a React Folder
1. npm create vite@latest . -- --template react
2. Click react
3. Click JavaScript
4. and install npm packages

## How to Connect to GitHub
1. git init
2. git add .
3. git commit - "name of the git"
4. create repo in github untick readme and git core
5. type in git remote in the terminal from github (copy and paste)
6. git branch -M main
7. git push -u origin main

## Home.jsx and Home.scss 
The Kanban board and its styling is from youtube and is generated by AI, will still need to learn how to do draggables independently.